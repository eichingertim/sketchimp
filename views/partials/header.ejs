<!DOCTYPE html>
<html lang="en">

<head>

    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <link href="/app/resources/css/fontawesome-all.min.css" rel="stylesheet">
    <link href="/app/resources/css/bootstrap.min.css" rel="stylesheet">
    <link href="/app/resources/css/jquery.mCustomScrollbar.css" rel="stylesheet">
    <link href="/app/resources/css/main.css?rev=@@hash" rel="stylesheet">
    <link href="/app/resources/css/main-responsive.css?rev=@@hash" rel="stylesheet">

</head>

<body>

    <nav class="navbar" role="navigation">
        <div class="container-fluid">
            <div class="navbar-menu">
                <% if(title !== "Login" && title !== "Register"){ %>
                    <% if(title !== "username"){ %>
                        <a href="/user/@me" class="btn btn-primary">My Profile</a>
                    <% } %>
                    <a href="/user/logout" class="btn btn-primary">Logout</a>
                <% } %>
            </div>
        </div>
    </nav>